name: First Workflow
on: workflow_dispatch
jobs:
  first-Job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello World"
      - name: Print Evening
        run: echo "Happy World"
